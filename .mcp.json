{
  "mcpServers": {
    "railway": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@railway/cli", "mcp"],
      "env": {
        "RAILWAY_API_TOKEN": "${RAILWAY_API_TOKEN}"
      }
    }
  }
}

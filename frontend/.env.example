# Frontend environment (M1). Vite only exposes vars prefixed with VITE_.

# Railway public URL of the backend service.
VITE_API_BASE_URL=http://localhost:8000

# Supabase project (the AR-Internal-Tools project).
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=

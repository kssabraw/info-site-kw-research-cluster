# Netlify deploy config. In the Netlify UI set the base directory to `frontend/`;
# the paths below are then relative to that base.
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routes resolve.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
